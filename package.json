{
  "name": "alim-space-game",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "firebase": "^10.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "typescript": "^5.2.0"
  }
}
